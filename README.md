# Test

This repository holds sample `.txt` files. This is the index.

## Files

- `branch_test.txt` — holds the text `TEST!`. It tests a branch workflow.
- `commit_test.txt` — holds the text `test-commit.` and `asd.` on separate lines. It tests a commit workflow.
- `fshell.txt` — holds the text `fshell!!!`. It tests a shell workflow.
- `test.txt` — holds a longer sample: `Hello, World!`, several `Test!` lines, a separator line, and `Test from a!.`. It is the original sample file.
- `test_2.txt` — holds the text `Test!`. It is the second sample file.
- `test_3.txt` — holds the text `Test!`. It is the third sample file.
- `test_4.txt` — holds the text `Test!`. It is the fourth sample file.
